{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "collector/Dockerfile.metrics",
    "watchPatterns": [
      "collector/**"
    ]
  },
  "deploy": {
    "startCommand": "python run_railway_refresh_cycle.py",
    "restartPolicyType": "NEVER"
  }
}
